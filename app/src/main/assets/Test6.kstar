// Basic operators and for bucle (Semantically valid, it will
// not compile due to the lack of main() function

class MathUtils {
    function int square() {
        int result = 0;
        for (int i = 0; i < 10; i = i + 1) {
            result = result + i * i;
        }
        return result;
    }
}
